<Window x:Class="LightON_Final.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LightON_Final"
        mc:Ignorable="d" x:Name="RingWindow"
        Title="LightON Overlay"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Opacity="{Binding EffectiveOpacity, FallbackValue=1.0}">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="{Binding RingSize, FallbackValue=0.6}" 
                          ScaleY="{Binding RingSize, FallbackValue=0.6}"/>
        </Grid.RenderTransform>
        
        <!-- LAYER 1: Outer glow (large blur, softer) -->
        <Border CornerRadius="200">
            <Border.BorderThickness>
                <Binding Path="RingThickness">
                    <Binding.Converter>
                        <local:ThicknessMultiplierConverter Multiplier="200"/>
                    </Binding.Converter>
                </Binding>
            </Border.BorderThickness>
            <Border.BorderBrush>
                <SolidColorBrush Color="{Binding LightColor, FallbackValue=White}"/>
            </Border.BorderBrush>
            <Border.Effect>
                <BlurEffect Radius="80" RenderingBias="Quality"/>
            </Border.Effect>
        </Border>
        
        <!-- LAYER 2: Medium outer glow -->
        <Border CornerRadius="200">
            <Border.BorderThickness>
                <Binding Path="RingThickness">
                    <Binding.Converter>
                        <local:ThicknessMultiplierConverter Multiplier="180"/>
                    </Binding.Converter>
                </Binding>
            </Border.BorderThickness>
            <Border.BorderBrush>
                <SolidColorBrush Color="White"/>
            </Border.BorderBrush>
            <Border.Effect>
                <BlurEffect Radius="50" RenderingBias="Quality"/>
            </Border.Effect>
        </Border>
        
        <!-- LAYER 3: Inner glow -->
        <Border CornerRadius="200">
            <Border.BorderThickness>
                <Binding Path="RingThickness">
                    <Binding.Converter>
                        <local:ThicknessMultiplierConverter Multiplier="150"/>
                    </Binding.Converter>
                </Binding>
            </Border.BorderThickness>
            <Border.BorderBrush>
                <SolidColorBrush Color="White"/>
            </Border.BorderBrush>
            <Border.Effect>
                <BlurEffect Radius="25" RenderingBias="Quality"/>
            </Border.Effect>
        </Border>
        
        <!-- LAYER 4: Sharp bright core -->
        <Border CornerRadius="200">
            <Border.BorderThickness>
                <Binding Path="RingThickness">
                    <Binding.Converter>
                        <local:ThicknessMultiplierConverter Multiplier="120"/>
                    </Binding.Converter>
                </Binding>
            </Border.BorderThickness>
            <Border.BorderBrush>
                <SolidColorBrush Color="White"/>
            </Border.BorderBrush>
            <Border.Effect>
                <BlurEffect Radius="8" RenderingBias="Quality"/>
            </Border.Effect>
        </Border>
        
        <!-- LAYER 5: Ultra-bright center (almost no blur) -->
        <Border CornerRadius="200">
            <Border.BorderThickness>
                <Binding Path="RingThickness">
                    <Binding.Converter>
                        <local:ThicknessMultiplierConverter Multiplier="100"/>
                    </Binding.Converter>
                </Binding>
            </Border.BorderThickness>
            <Border.BorderBrush>
                <SolidColorBrush Color="White"/>
            </Border.BorderBrush>
            <Border.Effect>
                <BlurEffect Radius="2" RenderingBias="Quality"/>
            </Border.Effect>
        </Border>
    </Grid>
</Window>
